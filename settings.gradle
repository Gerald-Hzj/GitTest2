pluginManagement {
    repositories {
        maven {
            url "${nexusUrl}"
        }
        gradlePluginPortal()
        maven {
            url "${nexusUrl}"
        }
    }
}

rootProject.name = 'OffcnStudentManageAPI'
